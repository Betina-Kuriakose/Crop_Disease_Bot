flask==3.0.0
flask-cors==4.0.0
pandas==2.1.4
scikit-learn==1.3.2
numpy==1.24.3
nltk==3.8.1
transformers==4.35.0
torch==2.1.0
sentencepiece==0.1.99

# Translation support
googletrans==4.0.0rc1
langdetect==1.0.9

# Voice features (optional - app works without these)
SpeechRecognition==3.10.0
pyttsx3==2.90
gTTS==2.5.1
# Note: PyAudio is required for microphone input but can be tricky on Windows
# If you get PyAudio errors, the app will still work but voice input won't be available
# To install PyAudio on Windows: pip install pipwin && pipwin install pyaudio
# Or download wheel from: https://www.lfd.uci.edu/~gohlke/pythonlibs/#pyaudio

# Optional: for loading environment variables
python-dotenv==1.0.0

# Database
pymongo==4.6.1

