from typing import Optional


def transcribe_audio(audio_bytes: bytes, language: Optional[str] = "en") -> str:
    # Placeholder STT: Returns fixed text. Replace with real STT (e.g., Vosk, Whisper).
    return "[placeholder transcription] Please describe your crop issue."


